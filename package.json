{
  "private": true,
  "scripts": {
    "capture:tree-layout": "node coa_scraper/scripts/capture-builder-tree-layout.mjs",
    "pipeline": "npm --prefix coa_scraper run pipeline",
    "scraper:test": "npm --prefix coa_scraper run unit-test",
    "scraper:validate": "npm --prefix coa_scraper run validate"
  }
}
